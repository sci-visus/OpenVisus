version: 2

jobs:


  # ///////////////////////////////// manylinux
  linux-3.8:
    docker:
      - image: visus/travis-image
        environment:
          PYTHON_VERSION: 3.8
    steps:
      - checkout
      - run: 
          name: Build OpenVisus
          command: scripts/build_circleci.sh
  
  linux-3.7:
    docker:
      - image: visus/travis-image
        environment:
          PYTHON_VERSION: 3.7
    steps:
      - checkout
      - run: 
          name: Build OpenVisus
          command: scripts/build_circleci.sh
            
  linux-3.6:
    docker:
      - image: visus/travis-image
        environment:
          PYTHON_VERSION: 3.6
    steps:
      - checkout
      - run: 
          name: Build OpenVisus
          command: scripts/build_circleci.sh

workflows:
  version: 2
  OpenVisusWorkflow:
    jobs:
      - linux-3.8    
      - linux-3.7
      - linux-3.6
